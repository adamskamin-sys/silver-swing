{
  "name": "silver-swing-dashboard",
  "version": "0.1.0",
  "description": "Read-only status dashboard for the silver-swing bot (spec §10 + §12 step 7)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-session": "^1.18.0",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
